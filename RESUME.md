# 【姓名】

> 求职意向：后端开发 / 推荐策略工程 / AI 应用工程师  
> 手机：【电话】｜邮箱：【邮箱】｜GitHub：【链接】｜城市：【城市】

---

## 教育背景

**【学校】｜【专业】｜本科/硕士**　　【入学年】—【毕业年】

- GPA / 相关课程 / 荣誉（按需补充）

---

## 专业技能

- **语言与框架**：Java（Spring Boot）、Python；熟悉 Redis、MySQL、Kafka、Dubbo
- **推荐与策略**：延迟队列、分布式锁、频控、UCB 多臂老虎机工程化、实验配置（Apollo）
- **AI / RAG**：混合检索（BM25 + 向量 + RRF）、父子分块、ReAct Agent、失败记忆、LLM-as-Judge 评测体系
- **工程能力**：高并发消费优化、幂等与降级设计、可观测打点、配置化与灰度

---

## 实习经历

### 【公司名】｜推荐 / 语音匹配后端　　【起止时间】

负责语音匹配链路中的付费转化弹窗、延迟策略与实验取数相关能力，覆盖延迟队列、分布式互斥、在线学习策略落地与平台吞吐优化。

#### 1. 超级匹配延迟弹窗（免费男转化付费）

- **背景**：面向免费男性用户的付费转化玩法——延迟一段时间后弹窗展示高质量女性候选；男性感兴趣则付费完成匹配。热门候选稀缺、易被并发流程争抢，需保证「点过去对方确为我预留」。
- **方案**：
  - ZSet 延迟闭环：过期任务清理、Lua 批量原子弹出、独立线程池并发执行推荐；批量大小与消费开关 Apollo 可调，积压自适应、过载可止血且任务不丢。
  - 推荐前存在性预检 + 双向预订锁 + 延迟期间 TTL 续期；锁冲突换候选；余额不足走引流降级编排。
  - 召回白名单双语义：调试只保留白名单，生产剔除内部账号。
- **收获**：厘清「秒级预订锁」与「资金正确性」职责边界——正确性由频控去重与支付幂等兜底，锁负责体验互斥。

#### 2. 免费男性延迟匹配 UCB 策略工程化

- **背景**：免费用户即时推送接通率低，延迟过短质量差、过长易流失；将延迟档位建模为 7 臂老虎机（20–50s），算法定公式与 reward，本人负责后端工程化。
- **方案**：
  - UCB1：`score = mean + α√(ln N / count)`，冷启动 count<3 优先试臂；按付费等级 HIGH/MID/LOW 分桶独立统计，Redis Hash 按 `modelVersion` 灰度可重置。
  - reward：`ln(成交卡片总价+1)`，组合卡型硬编码真实打包价；支付 / 挂断回调 Lua 原子结算，pending TTL + 定时清理双层防脏。
  - 全链路失败（资格/配置/Redis/写 pending）一律降级固定延迟并分原因打点——策略可实验，稳定性无条件。
- **收获**：能讲清 UCB vs ε-greedy、冷启动防除零死锁、以及「短操作靠 Redis 原子性、长任务才上线程池」的并发选型。

#### 3. 可视化平台 Kafka 消费与查询优化

- **背景**：推荐全链路 debug 消息（单条上千行明细）落 MySQL 供回放；原逐条 INSERT + 同步 Hive 转发导致 consumer lag 飙升，查询轮询占满 HTTP 线程。
- **方案**：
  - 批内合并 500 条批量 INSERT；Hive 回写迁出消费线程（异步批量 + 重试）；失败不 ack、`DuplicateKey` 幂等成功；运维改为 pause/resume 容器，禁止「跳过落库仍 ack」。
  - simulate 接口改异步返回 requestId；列表 N+1 改为批量 IN + 内存组装。
- **结果**：消费吞吐提升数倍，lag 高峰归零，排障恢复近实时；主链路 at-least-once，Hive 旁路尽力而为可补偿。

#### 4. 实验策略 Agent（取数 SQL 自助生成）

- **背景**：策略评估原需人工写 Hive SQL + 导表记飞书，单次约半天；先做跑批平台再上 Agent 按自然语言生成取数 SQL。
- **方案**：
  - 双线程池（执行池 Abort 保护 Hive / 队列池 CallerRuns 背压）；批量跑 SQL + 异步写飞书，端到端耗时约降 **89%**；cron 存库 + 动态调度，改周期不重启。
  - 双知识库：表文档（DDL/口径/参考 SQL）向量检索；指标与规范库作常驻提示词防口径漂移。约 200 文档用 SQLite + numpy 精确余弦（规模驱动选型，非盲目上向量库）。
  - 链路：需求归一化 → top-k 检索 → 组装上下文 → GLM 生成 → Hive EXPLAIN + 试跑校验。
- **评测**：50 条真实历史需求（以核实结果判卷）准确率 **90%**；20 条越界拒答率 **95%**；badcase 回流转述表，同类错误可收敛。

---

## 个人项目

### Agentic-RAG-ZH｜中文 Agentic-RAG 框架（自研）

> 参考业界混合检索 + Agent 工具循环模式自研，面向中文新闻领域知识库问答（8.9 万篇文档 / 35 万向量块）。  
> 仓库：【GitHub 链接】｜技术栈：Python、ChromaDB、BGE、jieba+BM25、OpenAI 兼容 LLM 接入层

- **混合检索**：中文父子分块（child 检索 / parent 生成）；BM25(jieba) + bge-small-zh 双路召回、RRF(k=60) 融合、按 parent 去重；Hit@5=0.95，MRR=0.74。
- **Agent**：手写 ReAct 工具循环（检索 query 自主改写，≤4 步），function calling 接入；答案带来源引用，库外问题拒答（拒答准确率 1.0）。
- **失败记忆**：失败轨迹经 LLM 根因归因（outcome / root_cause / lesson 结构化）写入向量库，新任务按相似度阈值召回注入 prompt；机制经消融验证（召回相似度 1.0、注入改变生成）。
- **三层评测**：基于 CRUD-RAG 自建 42 条中文黄金集（单跳 / 多跳 / 拒答）；检索层 Hit@k / MRR / Recall@k / nDCG@k + 答案层关键词规则 + **GLM 跨源 LLM-as-Judge 语义判分（判对率 1.0）**；用 Judge 仲裁规则评测假阴性（9 条误判中 8 条证实），并发现规则口径存在 ±5pp 运行间方差——据此设计三口径指标体系。运行历史落盘可对比；评测全程成本 < 3 元。

---

## 自我评价（可选）

熟悉推荐链路里「延迟、锁、频控、降级」类工程问题，能把在线学习策略（UCB）稳定落地；同时具备 RAG / Agent 全链路自研与评测意识，重视可复现实验与失败归因，而不是只堆模型调用。

---

## 填写说明（写完可删）

1. 补全顶部姓名、联系方式、教育背景、公司名与实习时间、GitHub。
2. 实习条目已按飞书文档《实验策略与语音匹配服务》压缩为简历粒度；面试深挖仍以飞书原文为准。
3. Agentic-RAG **只写代码已落地且可背书的内容**。禁止写：LangGraph、Plan-and-Execute、上下文压缩/token 预算、工具调用 F1、faithfulness（实际是 semantic_correct + relevancy）、未跑消融的 Cross-Encoder、「人工校对」（实际是自动关键词 + 跨源 Judge 复核）。
4. 数字（Hit@5 / MRR / 拒答 / Judge / 成本）须与本地 eval history 一致；投递前再对一下最新一次跑分。

